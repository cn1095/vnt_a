# 聊天室修改说明

## 本次实现范围

本次将房间页面中的“聊天室”占位页替换为基于 VNT 虚拟组网的去中心化聊天室。聊天室不依赖中心聊天服务器，客户端通过 VNT 虚拟 IP 直接发现、加入、收发消息和共享文件。

## 主要文件

- `lib/pages/room_page.dart`：接入新的聊天室 TAB，不改变设备 TAB、路由 TAB 和现有 VNT 连接逻辑。
- `lib/chat/chat_tab.dart`：聊天室列表、创建房间、加入房间、聊天会话、成员列表、文件共享、音视频/屏幕/远协入口。
- `lib/chat/chat_peer_service.dart`：TCP 服务端、peer 探测、加入校验、消息转发、历史同步和信令分发。
- `lib/chat/chat_file_server.dart`：本机 `HttpServer` 文件共享，支持 Range 请求和大文件流式下载。
- `lib/chat/chat_history_store.dart`：本地聊天记录，按组网和房间保存，单房间最大 20MB。
- `lib/chat/chat_identity.dart`：组网指纹和房间密码摘要。
- `lib/chat/chat_platform_permissions.dart`：多平台音视频、屏幕共享、远程协助权限处理。
- `android/app/src/main/java/top/wherewego/vnt_app/VntRemoteAssistAccessibilityService.java`：Android 远程协助无障碍服务占位。

## 通信设计

- 发现和聊天端口：`39271`
- 文件共享端口：`39272`
- WebRTC 信令预留端口：`39273`

同组网隔离使用摘要指纹：

```text
sha256(token + serverAddress + groupPassword + encryptionAlgorithm + useChannelType + salt)
```

客户端只交换摘要，不发送 token 或组网密码明文。

## 已支持能力

- 创建聊天室
- 房间密码校验
- 同组网在线聊天室发现
- 加入聊天室
- 文本消息
- 系统消息
- 本地聊天记录
- 加入后历史消息分片同步
- 文件、图片、视频、语音文件共享
- 文件夹共享和文件夹列表查看
- 大文件流式下载和 Range 请求
- 语音聊天、视频聊天、屏幕共享、远程协助信令入口

## 平台权限说明

### Android

- 已添加麦克风、摄像头、媒体读取权限。
- 远程协助已增加无障碍服务检测。
- Android 无障碍权限不是普通运行时权限，应用不能直接弹授权框，只能检测是否开启并跳转到系统无障碍设置。
- 已新增 `VntRemoteAssistAccessibilityService` 作为远程协助无障碍服务占位，后续真正执行点击、滑动、输入时通过该服务实现。

### iOS

- 暂时只允许查看对方共享的屏幕。
- 不发起屏幕共享，不做远程控制。
- 已添加本地网络、相册、麦克风、摄像头权限说明，便于后续扩展。

### macOS

- 已添加麦克风、摄像头、屏幕录制说明。
- 已添加 camera 和 audio-input entitlements。
- 屏幕共享和远程协助仍需要用户在系统设置中允许屏幕录制/辅助功能。

### Windows / Linux

- 聊天和文件共享走 `dart:io`，不需要额外中心服务。
- 远程协助和屏幕采集后续需要结合平台原生能力处理权限和输入注入。
- Linux Wayland 下屏幕采集和输入控制可能受到系统限制。

## 注意事项

- 当前没有引入 `flutter_webrtc` 依赖，避免与现有 `flutter_rust_bridge 2.1.0` 的 `web` 依赖版本冲突。
- 当前没有引入 `permission_handler` 依赖，避免为暂未实现的音视频采集提前增加桌面端插件风险；Android 远程协助无障碍检测使用现有原生 MethodChannel。
- 后续真正实现实时语音、视频和屏幕共享时，需要单独处理 WebRTC 依赖兼容性；优先评估升级 `flutter_rust_bridge` 对生成代码和 Rust 桥接的影响。
- 当前环境没有 `dart` / `flutter` 命令，因此未在本机执行格式化、分析或编译。
- 已执行 `git diff --check`，没有发现空白补丁问题。
- 已按要求执行 `git add -A`。
