# 聊天室待开发实现

本文记录当前聊天室第一版之后仍需继续开发的内容，便于后续按模块推进。当前已完成基础聊天室闭环：聊天室发现、创建、加入、文本消息、本地历史、历史同步、文件共享、平台权限入口和音视频/屏幕/远协信令入口。以下内容尚未完整落地。

## 1. 实时语音聊天

### 当前状态

- 已有语音聊天按钮。
- 已有音视频权限申请入口。
- 已有 `callInvite` 信令入口。
- 当前未引入 `flutter_webrtc` 依赖，原因是 `flutter_webrtc >=0.11.7` 依赖 `web ^1.0.0`，与当前 `flutter_rust_bridge 2.1.0` 依赖的 `web ^0.5.0` 冲突。
- 当前未引入 `permission_handler` 依赖，避免在真实音视频采集尚未实现前影响 Windows 等桌面端启动；Android 后续需要在原生媒体采集模块中请求麦克风和摄像头运行时权限。
- 尚未建立真正的 WebRTC 音频通话。

### 需要实现

- 先解决 WebRTC 依赖兼容：
  - 方案一：评估升级 `flutter_rust_bridge` 到兼容 `web ^1.0.0` 的版本，并重新验证 FRB 生成代码。
  - 方案二：寻找兼容 Flutter 3.24.4、Android 5 且不冲突的 `flutter_webrtc` 旧版本。
  - 方案三：将音视频能力做成平台原生插件，避免影响现有 FRB 依赖。
- 引入并封装 WebRTC 音频会话管理。
- 创建 `RTCPeerConnection`。
- 采集本机麦克风音频流。
- 通过聊天室 TCP 信令交换 SDP offer / answer。
- 通过聊天室 TCP 信令交换 ICE candidate。
- 建立点对点音频通话。
- 支持接听、拒绝、挂断、静音、扬声器切换。
- 支持通话状态展示：邀请中、连接中、通话中、已结束、失败。
- 支持异常断线后释放音频设备。

### 建议文件

- `lib/chat/call/chat_call_models.dart`
- `lib/chat/call/chat_call_service.dart`
- `lib/chat/call/chat_audio_call_page.dart`

### 验收标准

- 两台同 VNT 组网设备可以发起和接听语音聊天。
- 断开 VNT 或退出聊天室后通话自动结束。
- Android 5 可以正常申请麦克风权限。
- Windows、Linux、macOS 至少能进入通话流程并建立音频。
- iOS 当前策略如仍只允许观看屏幕共享，则语音发起入口应禁用或提示不可用。

## 2. 实时视频聊天

### 当前状态

- 已有视频聊天按钮。
- 已有摄像头权限申请入口。
- 已有 `callInvite` 信令入口。
- 尚未建立真正的视频采集、渲染和 WebRTC 视频通话。

### 需要实现

- 采集本机摄像头视频流。
- 渲染本地预览。
- 渲染远端视频。
- 复用 WebRTC 信令通道交换 SDP / ICE。
- 支持开关摄像头、切换摄像头、静音、挂断。
- 支持视频窗口布局，移动端和桌面端分别适配。
- 支持视频通话中降级为语音。

### 建议文件

- `lib/chat/call/chat_video_call_page.dart`
- `lib/chat/call/chat_media_renderer.dart`

### 验收标准

- 两台同 VNT 组网设备可以互相看到视频画面。
- 退出聊天室后摄像头释放。
- Android 5 权限申请和摄像头调用正常。
- macOS 首次使用能触发系统摄像头权限。
- Windows / Linux 能正常打开默认摄像头。

## 3. 屏幕共享

### 当前状态

- 已有屏幕共享按钮。
- 已有屏幕共享权限提示。
- iOS 当前设计为只能查看对方屏幕共享。
- 尚未实现屏幕采集和远端画面渲染。

### 需要实现

- 基于 WebRTC 增加屏幕流 track。
- 桌面端实现屏幕或窗口选择。
- Android 使用 MediaProjection 请求录屏授权。
- iOS 仅实现远端屏幕流观看，不实现本机屏幕共享。
- macOS 提示用户开启屏幕录制权限。
- Linux 需要区分 X11 / Wayland，Wayland 下可能依赖门户授权。
- 支持共享中状态条：正在共享屏幕，点击停止。
- 支持远端观看页：适应窗口、原始比例、全屏。

### 建议文件

- `lib/chat/screen/chat_screen_share_service.dart`
- `lib/chat/screen/chat_screen_share_view.dart`

### 平台注意

- Android 5 支持 MediaProjection，但录屏授权必须由系统弹窗确认。
- iOS 普通应用内屏幕采集能力受限，完整屏幕共享通常需要 Broadcast Extension。
- macOS 必须在系统设置中允许屏幕录制。
- Linux Wayland 可能无法直接采集，需要 xdg-desktop-portal。

### 验收标准

- Windows / macOS / Linux 至少一个桌面平台可以共享屏幕给另一台设备观看。
- Android 可以发起屏幕共享并被桌面端观看。
- iOS 可以观看远端屏幕共享。
- 停止共享后远端画面及时关闭。

## 4. 远程协助

### 当前状态

- 已有远程协助按钮。
- 已有远程协助信令入口。
- 已有远程协助一次性邀请确认卡片，被控端可以同意或拒绝。
- Android 已新增无障碍服务占位。
- Android 已能检测无障碍权限是否开启，并能跳转系统无障碍设置。
- 尚未实现真正的鼠标、键盘、触控和输入注入。

### 需要实现

- 远程协助邀请流程：请求协助、接受、拒绝、结束。（请求、接受、拒绝已完成；结束待开发）
- 权限确认弹窗：被控端必须明确确认本次远控。（聊天邀请卡片确认已完成；独立弹窗待增强）
- 控制通道：使用 WebRTC DataChannel 或聊天室 TCP 传输控制事件。
- 鼠标移动、左键、右键、滚轮。
- 键盘按键和组合键。
- 触屏点击、滑动、长按。
- 剪贴板同步可作为后续可选能力。
- 被控端醒目提示：正在被远程协助，点击停止。
- 所有控制事件应绑定当前会话，离开聊天室或断线后立即失效。

### Android 实现方向

- 使用 `VntRemoteAssistAccessibilityService` 注入手势。
- Android 7 及以上可用 `dispatchGesture` 实现手势注入。
- Android 5 / Android 6 无完整 `dispatchGesture`，需要降级：
  - 可以只支持观看屏幕。
  - 或仅支持有限的辅助功能动作。
  - UI 必须明确提示“当前系统版本不支持完整远控”。

### Windows 实现方向

- 后续可通过原生插件调用 Win32 API。
- 鼠标：`SendInput`
- 键盘：`SendInput`
- 截屏兼容 Win7 时不能只依赖 DXGI Desktop Duplication，需准备 GDI fallback。

### macOS 实现方向

- 需要辅助功能权限。
- 鼠标键盘注入可通过 CGEvent。
- 屏幕采集需要屏幕录制权限。

### Linux 实现方向

- X11 可通过 XTest。
- Wayland 下全局输入注入通常受限制，需要桌面环境或门户支持。

### iOS 实现方向

- 当前不做远程控制。
- 只保留查看对方屏幕共享。

### 验收标准

- Android 开启无障碍服务后，被控端确认授权，控制端可点击和滑动。
- Windows 被控端确认授权后可鼠标移动、点击、键盘输入。
- 权限未开启时不会进入远控会话。
- 远控过程中被控端始终有明显提示并能立即停止。

## 5. 语音消息录制

### 当前状态

- 当前“语音”文件可以作为普通文件共享。
- Android 已支持按住说话、录音、松开发送语音文件消息。
- Android 使用原生 `MediaRecorder` 录制 m4a 到缓存目录，然后通过聊天室文件共享服务发送。
- Android 已支持在聊天气泡中直接播放/停止语音消息。
- 非 Android 平台暂时仍发送语音消息元数据占位，后续需要接入桌面/iOS 原生录音。

### 需要实现

- 录音权限申请。（Android 已完成）
- 录音开始、取消、完成。（Android 已完成）
- 录音文件编码格式选择。（Android 已使用 m4a/AAC）
- 语音时长计算。（Android 已完成）
- 消息气泡展示语音长度。（已完成）
- 点击播放、暂停、继续播放。（Android 已完成基础播放/停止）
- 播放进度展示。
- 录音文件仍通过文件共享 HttpServer 或分片传输发送。（Android 已通过 HttpServer 共享）

### 验收标准

- Android 5 可以录制并发送语音消息。（待 CI/真机验证）
- 桌面端可以播放收到的语音消息。
- 权限被拒绝时提示清晰。

## 6. 图片和视频预览

### 当前状态

- 图片和视频作为文件消息共享。
- 图片消息已支持聊天气泡内预览。
- 图片消息已支持点击查看大图、缩放和下载。
- 视频消息已支持独立视频气泡、外部播放器打开和下载。
- 尚未实现应用内视频播放器。

### 需要实现

- 图片消息生成缩略图。
- 图片气泡显示缩略图。（已完成基础网络预览）
- 点击图片进入查看器，支持缩放和保存。（已完成基础查看和下载）
- 视频消息显示封面、时长和大小。
- 点击视频进入播放器。（已完成外部播放器打开，应用内播放器待开发）
- 下载失败、源设备离线时给出明确提示。

### 验收标准

- 图片消息在聊天列表中不是普通文件图标，而是缩略图。
- 视频消息可点击播放或先下载后播放。
- 大图片不会阻塞 UI。

## 7. 文件夹递归浏览和批量下载

### 当前状态

- 已支持共享文件夹。
- 已支持查看共享文件夹文件列表。
- 已支持递归浏览子目录。
- 已支持在子目录中下载文件。
- 已支持下载当前文件夹到本地目录，客户端递归创建目录并逐个流式下载文件。
- 尚未实现多选下载和服务端 zip 打包下载。

### 需要实现

- 文件夹树 API。（已完成基础 `path` 参数浏览）
- 子目录点击进入。（已完成）
- 面包屑路径导航。（已完成上级目录返回，完整面包屑待增强）
- 多选文件。
- 批量下载。（已完成当前文件夹递归下载；多选待开发）
- 文件夹下载可选方案：
  - 服务端临时 zip 流式打包。
  - 客户端递归下载并创建目录。（已完成）
- 下载队列和进度。
- 断点续传记录。

### 验收标准

- 可以浏览多级目录。
- 可以下载子目录中的文件。
- 可以下载当前文件夹并保留子目录结构。
- 大文件下载支持断点续传。
- 多文件下载不会造成内存暴涨。

## 8. 聊天记录数据库化

### 当前状态

- 当前使用 JSONL 保存本地记录。
- 尚未使用 Isar。

### 需要实现

- 选择兼容 Flutter 3.24.4 / Dart 3.4 的 Isar 版本。
- 建立消息表、房间表、共享文件表。
- 每个聊天室独立记录。
- 保留 20MB 容量限制。
- 支持按时间分页加载。
- 支持消息去重索引。
- 支持退出聊天室删除本地记录。
- 支持组网配置删除时删除该组网全部聊天室记录。

### 验收标准

- 历史消息加载不卡 UI。
- 2 万条文本消息分页正常。
- 超过容量后自动删除旧消息。
- 删除房间或组网后本地数据清理干净。

## 9. 消息一致性和多节点同步

### 当前状态

- 当前房主负责转发消息。
- 加入时从房主同步历史。
- 多节点强一致性尚未实现。

### 需要实现

- 消息全局序列号或 Lamport Clock。
- 多来源历史同步。
- 消息 ACK。
- 断线重连后增量同步。
- 房主离线后的房主迁移。
- 所有成员离线后的聊天室销毁确认。
- 离线房间缓存清理。

### 建议方案

- 每条消息保留 `messageId`、`senderDeviceId`、`timestamp`、`sequence`。
- 房主分配房间内递增序号。
- 房主离线时按成员设备 ID 或加入时间选出新房主。
- 新房主广播 `ownerChanged`。
- 客户端重连后发送自己最后一条已知消息序号，请求增量同步。

### 验收标准

- 房主离线后聊天室不会立即不可用。
- 客户端断线重连后能补齐消息。
- 重复消息不会显示两次。

## 10. 安全和权限确认

### 当前状态

- 组网隔离使用摘要指纹。
- 房间密码使用摘要校验。
- 房间密码错误尝试已增加失败计数和短时间锁定。
- 远程协助仅有权限入口，还没有完整会话授权。

### 需要实现

- 聊天室协议版本字段。
- 房间密码尝试失败限制。（已完成基础限制：同设备/IP 连续失败 5 次锁定 5 分钟）
- 文件共享路径安全强化。
- 远程协助一次性会话授权。
- 屏幕共享和远程协助水印或顶部提示。
- 敏感操作日志。
- 可选端到端消息加密。

### 验收标准

- 不同组网不能互相发现聊天室。
- 错误密码不能加入。
- 共享文件不能通过 `../` 访问共享目录外内容。
- 远程协助必须由被控端明确确认。

## 11. UI 和体验完善

### 当前状态

- UI 已接入现有主题样式。
- 基础列表、聊天页、成员列表、共享提示条已可用。
- 聊天输入栏已支持分类 emoji 表情面板。
- 多媒体通话和共享文件页仍需独立页面。

### 需要实现

- 独立共享文件页。
- 独立下载管理页。
- 音视频通话页。
- 屏幕共享观看页。
- 远程协助控制页。
- 消息发送失败重试。
- 文本消息发送失败时保留输入内容，避免消息丢失。（已完成基础保护）
- 聊天室在线成员状态刷新。
- 文件下载速度和剩余时间。
- 空状态、错误状态、权限状态统一设计。

### 验收标准

- 移动端和桌面端布局不溢出。
- 长文件名、长消息、窄屏都能正常显示。
- 弱网下失败状态明确。

## 12. 自动化测试和构建验证

### 当前状态

- 当前本地环境没有 `dart` / `flutter` 命令。
- 尚未本机运行格式化、分析或编译。
- 已执行 `git diff --check`。

### 需要实现

- GitHub Actions 中增加 Flutter 3.24.4 构建矩阵。
- Android minSdk 21 构建验证。
- Windows、Linux、macOS 构建验证。
- 聊天协议模型单元测试。
- 历史记录读写测试。
- 文件 Range 请求测试。
- TCP 探测超时测试。
- UI smoke test。

### 验收标准

- CI 能构建 Android、Windows、Linux、macOS。
- 聊天核心模型测试通过。
- 文件服务器 Range 测试通过。
- 不影响现有 VNT 连接功能。

## 建议后续开发顺序

1. 先补 WebRTC 信令处理和语音通话。
2. 再补视频通话。
3. 再补屏幕共享观看和发起。
4. 再补远程协助控制通道和平台原生注入。
5. 再将 JSONL 历史记录迁移到 Isar。
6. 最后增强多节点一致性、房主迁移和完整下载管理。
